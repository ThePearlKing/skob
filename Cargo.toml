[package]
name = "skob"
version = "0.1.0"
edition = "2021"
description = "soft things that live in your terminal"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
